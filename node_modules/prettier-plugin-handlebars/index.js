
const { languages } = require('./languages');
const { parsers } = require('./parser');
const { printers } = require('./printer');

module.exports = {
  languages,
  parsers,
  printers,
  options: {
    handlebarsIndentWidth: {
      type: 'int',
      category: 'Handlebars',
      default: 2,
      description: 'Number of spaces to use for indentation in Handlebars templates'
    },
    handlebarsPreserveHelperSpacing: {
      type: 'boolean',
      category: 'Handlebars',
      default: true,
      description: 'Preserve spacing around helper arguments'
    }
  }
};
